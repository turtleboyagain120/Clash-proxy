# proxy-bench

CLI to benchmark proxies: latency, speed, reliability.

## Install
```bash
pip install proxy-bench
```

## Usage
```bash
proxy-bench test socks5://127.0.0.1:1080 --speed --timeout 5
```

Keywords: proxy-benchmark, vpn-test, speedtest-proxy

